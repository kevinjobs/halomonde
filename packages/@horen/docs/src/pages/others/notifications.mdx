import { notifications, Notifications } from '@horen/notifications';
import { randomInt } from '@horen/utils';

# Notifications

### Base

<Notifications />

<button onClick={() => notifications.show({type: ['success', 'error', 'warning', 'info'][randomInt(0,4)], message: (new Date()).valueOf()})}>show notice</button>